<%= simple_form_for [:manage, @group], html: { class: 'form-horizontal' } do |f| %>
  <div class="form-group">
    <%= f.input :title %>
    <%= f.input :uid %>
    <%= f.submit %>
  </div>
<% end %>
