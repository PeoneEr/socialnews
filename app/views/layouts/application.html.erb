<!DOCTYPE html>
<html>
  <head>
    <title>Фиды социальных сетей</title>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body>

    <div class="container">
      <h2 class='header'>Список последних записей</h2>
      <h4 class="header">Последнее обновление: <%= Time.zone.parse($redis.get 'last_update').strftime('%H:%M') %></h4>

      <%= yield %>

    </div>

  </body>
</html>
